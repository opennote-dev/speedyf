export const ONE_MIB = 1024 * 1024;

export const DEFAULT_MAX_PAGES = 999;
export const DEFAULT_MAX_FILE_SIZE_BYTES = 49 * ONE_MIB; // 49MiB

export const GENERIC_MIME_TYPES = [
  "application/octet-stream",
  "binary/octet-stream",
  "",
];

export const MAX_CHUNK_SIZE_BYTES = 4 * ONE_MIB; // 4MiB Per Chunk
export const MAX_STREAMED_FILE_SIZE_BYTES = 2000 * ONE_MIB; // 2 GiB
